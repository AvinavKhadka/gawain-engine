fastapi==0.115.0
uvicorn==0.30.6
pyodbc==5.2.0
httpx==0.27.2
python-dotenv==1.0.1
pandas==2.2.3
tabulate==0.9.0
# Driver-analysis engine (local DuckDB extract + attribution / ML / changepoint).
# Floor constraints so wheels resolve across Python versions (incl. 3.13/3.14).
numpy>=1.26
duckdb>=1.1
scikit-learn>=1.5
